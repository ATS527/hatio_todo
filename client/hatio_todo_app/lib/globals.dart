/// This file is only for storing the jwt key
/// Testing won't support cookie data so we need to store the jwt key in the globals.dart file
/// Production code doesn't use this file and uses the cookie data instead

String jwtKey = "";
String projectId = "";
String todoId = "";
String userId = "";